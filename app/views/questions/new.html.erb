<div class="container">
  <h2 class="title-lg mb-sm">Новый вопрос</h2>
  <p class="mb">для <%= link_to fnickname(@user.nickname), user_path(@user.nickname), class: 'without-underline text-orange' %></p>

  <%= render 'users/errors_output', obj: @question %>
  <%= render 'form', question: @question %>
</div>
